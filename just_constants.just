# Constants used across justfiles

# Toolchain version required for cargo-check-external-types
# This tool depends on a specific rustdoc JSON format version
CARGO_CHECK_EXTERNAL_TYPES_TOOLCHAIN := "nightly-2025-10-18"
