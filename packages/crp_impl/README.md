# crp_impl

Implementation crate for [cargo-release-plan](https://crates.io/crates/cargo-release-plan).
Do not depend on this crate directly. Use the `cargo-release-plan` command line.
Neither package provides a supported Rust library API.
