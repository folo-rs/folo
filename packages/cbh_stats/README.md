# cbh_stats

Implementation crate for [`cargo-bench-history`](https://github.com/folo-rs/folo). Do
not depend on this directly — it carries the pure statistical primitives for the
analysis detectors and has no stable public API. Use the `cargo-bench-history` tool
instead.
