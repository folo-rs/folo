/// Represents the magnitude of an event occurrence.
///
/// Every `i64` value is valid. Scale fractional measurements into an integer unit, such
/// as milliseconds or nanoseconds, before observing them.
pub type Magnitude = i64;
