# cbh_codec

Implementation crate for [`cargo-bench-history`](https://github.com/folo-rs/folo). Do
not depend on this directly — it carries the gzip codec for stored objects and has no
stable public API. Use the `cargo-bench-history` tool instead.
