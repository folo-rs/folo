# cbh_analyze

Implementation crate for [`cargo-bench-history`](https://crates.io/crates/cargo-bench-history).

Do not depend on this crate directly; its surface is documented on and re-exported
through the `cargo-bench-history` binary crate.
