# cbh_render

Implementation crate for [`cargo-bench-history`](https://github.com/folo-rs/folo). Do
not depend on this directly — it renders analysis findings into terminal and Markdown
output and has no stable public API. Use the `cargo-bench-history` tool instead.
