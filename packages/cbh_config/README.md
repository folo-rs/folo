# cbh_config

Implementation crate for [`cargo-bench-history`](https://github.com/folo-rs/folo). Do
not depend on this directly — it carries the `.cargo/bench_history.toml` configuration
model and its TOML parsing/loading, and has no stable public API. Use the
`cargo-bench-history` tool instead.
