unstable_features = true

format_code_in_doc_comments = true
imports_granularity = "Module"
group_imports = "StdExternalCrate"

# Generated calibration data; the generator owns its exact bytes and `check` compares against them.
ignore = ["packages/cbh_stats/src/selection/table.rs"]
